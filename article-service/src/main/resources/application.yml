server:
  port: 8082

spring:
  application:
    name: article-service

  # 使用 H2 内存数据库（无需密码）
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:ai_knowledgehub;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password:

  # RabbitMQ（文章发布事件发送给 ai-service）
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true


mybatis-plus:

  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
    ddl-auto: create

logging:
  level:
    com.ai.knowledgehub.article: debug

article:
  ai-consumer:
    # 默认由 ai-service 消费文章发布事件，避免 article-service 与 ai-service 抢同一队列。
    enabled: false

