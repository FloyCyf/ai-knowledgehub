- 控制器统一从请求头 X-User-Id 和 X-User-Role 获取用户身份与角色，并在方法入口处执行未登录或无权限校验。
- 所有写操作服务方法均添加 @Transactional(rollbackFor = Exception.class) 注解以确保事务一致性。
- 实体到 VO 的转换统一使用 Spring BeanUtils.copyProperties 方法，且在 Service 层完成。
- 控制器内定义私有的 successResponse 和 errorResponse 辅助方法，构建包含 code、message、data 字段的统一响应结构。