- RESTful 接口统一返回包含 code、message、data 字段的 Map 结构响应体。
- Service 层通过私有通用方法 `incrementScore` 封装 Redis ZINCRBY 操作，不同业务动作（阅读、点赞等）调用该方法并传入特定分值常量。
- VO 类统一使用 Lombok 的 `@Builder`、`@Data`、`@NoArgsConstructor` 和 `@AllArgsConstructor` 注解组合。